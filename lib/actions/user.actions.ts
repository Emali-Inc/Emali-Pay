'use server';

export const SignIn = async () => {
    try {
       //Mutation /Modify the database  Make fetch
    } catch (error) {
        console.error(error);
    }
}

export const SignUn = async () => {
    try {
       //Mutation /Modify the database  Make fetch
    } catch (error) {
        console.error(error);
    }
}